[Unit]
Description=Run gha-runner-monitor check every 5 minutes

[Timer]
OnCalendar=*:0/5
# ハートビート判定に使うので間隔の揺れを詰める (domain-monitor と同じ理由)。
AccuracySec=10s
RandomizedDelaySec=20s
# 遡って埋めても意味が無い監視なので Persistent は付けない。
Persistent=false
Unit=gha-runner-monitor.service

[Install]
WantedBy=timers.target
